# 復旧データチェック補助ツール

## ファイル構成
- `recovery_check.html` — ブラウザ版メイン（Chrome/Edge のみ）
- `recovery_check.py`   — Python 版（OS レベルで動くため制限が少ない）

## ブランチ
- 開発中: `claude/cool-wright-Yy7bh`

## ツールの概要
外付けHDD・USBメモリから復旧したデータの状態をチェックする。
- 総フォルダー数・総ファイル数・総容量
- 正常ファイル数・破損ファイル数・復旧率
- サムネイル画像識別（詳細スキャンのみ）
- 結果を HTML レポートとして保存可能

## スキャンモード（HTML版）
| モード | 内容 |
|--------|------|
| 通常スキャン | 0バイト・読み取り不可の検出。高速 |
| 詳細スキャン | ヘッダー検証・末尾マーカー確認・サムネイル判定を追加。低速 |

## 対応済みフォーマット（ヘッダー検証）
```
画像:     jpg/jpeg/png/gif/webp/bmp/tiff/psd/ai
動画:     mp4/mov/m4v/mkv/webm/avi/wmv/flv
音声:     mp3/wav/m4a/flac/ogg
ドキュメント: pdf/docx/xlsx/pptx/odt/ods/odp/doc/xls/ppt
アーカイブ:   zip/rar/7z/gz/bz2
グラフィック: psd/ai
CAD:      dwg
メール:   pst/ost
```
上記以外は 1バイト読み取りチェックのみ。

## 破損判定の種類
| バッジ | 意味 |
|--------|------|
| 0バイト | ファイルサイズ = 0 |
| 読み取り不可 | ブラウザが読めない |
| ヘッダー異常（形式不一致） | 拡張子と実データの形式が違う |
| 末尾データ欠損（切断の可能性） | JPEG/PNG/PDF の終端マーカーなし |

## サムネイル識別
- 詳細スキャンのみ
- `createImageBitmap` で実寸を取得
- 幅・高さ ともに 100〜999px の画像をサムネイルとして分類

## 既知の制限
- **特殊属性フォルダー問題**: File System Access API の制限で、隠し属性・システム属性・ジャンクション経由のフォルダーはファイルが見えず「空フォルダー」扱いになる。Python 版なら OS レベルで読めるケースが多い。
- **内部破損の検出不可**: ファイル途中のビット破損は検出できない（ヘッダー・末尾が正常なら正常判定）。

## 結果カードの構成（HTML版）
1. チェック概要（対象パス・日時・モード）
2. サマリー（フォルダー数・ファイル数・容量・正常・破損・サムネイル数）
3. 復旧率（%・カラーバー・ステータス）
4. 破損ファイル一覧
5. サムネイル画像一覧（詳細スキャンのみ）
6. 読み取り不可フォルダー一覧
7. 空フォルダー一覧

## 動作環境
- HTML版: Windows の Chrome または Edge（Firefox・Safari 非対応）
- Python版: クロスプラットフォーム（Python 3.8+）
